# ---------------------------------------------------------------------------
# Example env-file for local development
# Copy to `.env` and adjust values as needed.
# ---------------------------------------------------------------------------

# Path to your private SSH key – used by the helper script *and* Docker (if
# you mount the file into containers).
# Leave empty to fall back to `~/.ssh/id_ed25519`.
#GIT_SSH_KEY_PATH=/home/$USER/.ssh/id_ed25519

# Optional: URL to your Ollama instance (large language models).
#OLLAMA_URL=http://192.168.2.10:11434/api/chat
