[build]
  command = "pip install -r requirements.txt && npm ci && npx tailwindcss -i input.css -o static/styles.css --minify && python freeze.py"
  publish = "build"

[build.environment]
  PYTHON_VERSION = "3.12"
